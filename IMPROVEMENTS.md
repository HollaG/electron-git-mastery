# Improvements to GitMastery app

## Potential Future Improvements

1. Create automated release CI pipeline (use semantic versioning)
2. UI/UX can still be improved
3. Add unit tests
4. Check for latest gitmastery latest version and show toast to inform user to update (check `gitmastery version` on app startup if setup is done)
